#include "triangle.h"

#define SIZE 40

int main(void)
{
	Triangle1(SIZE, '*');
	Triangle2(SIZE, 'o');
	Triangle3(SIZE, '$');
	Triangle4(SIZE, '#');
	Triangle5(SIZE, 'X');
	Triangle6(SIZE, 'a');

	return 0;
}